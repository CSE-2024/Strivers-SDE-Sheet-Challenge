#include <bits/stdc++.h> 
int kthLargest(vector<int>& nums, int size, int k)
{
	sort(nums.begin(),nums.end());
        int res;
        for(int i=nums.size()-1;k>0;i--){
            res=nums[i];
            k--;
        }
        return res;
}
